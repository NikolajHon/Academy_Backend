spring:
  datasource:
    url: jdbc:postgresql://fsa-honcharenko-psql.postgres.database.azure.com:5432/fsa-db?sslmode=require
    username: fsaadmin@fsa-honcharenko-psql
    password: P@ssword12345
    driver-class-name: org.postgresql.Driver

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
    generate-ddl: true
    hibernate:
      ddl-auto: update
    mapping-resources:
      - persistence/discussion-orm.xml

  profiles:
    active: keycloak

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri:   http://172.205.72.71/realms/Academia_project
          jwk-set-uri:  http://172.205.72.71/realms/Academia_project/protocol/openid-connect/certs

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql: TRACE
